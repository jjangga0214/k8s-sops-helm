apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.container.name }}-secrets
type: Opaque
data:
  {{ $environment := .Values.environment }}
  secrets.yaml: {{ "../../decrypted-ENV-secrets.yaml" | replace "ENV" $environment | .Files.Get | b64enc }}
